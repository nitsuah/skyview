services:
  web:
    build:
      context: .
    ports:
      - "8080:80"

  unit:
    image: node:20-alpine
    working_dir: /workspace
    volumes:
      - .:/workspace
    command: sh -lc "npm ci && npx vitest run --coverage"